server:
  port: 8081

spring:
  application:
    name: authentication-application

  datasource:
    url: jdbc:mysql://localhost:3306/authentication_application
    username: root
    password: root

  jpa:
    hibernate:
      ddl-auto: update
      format-sql : true
    show-sql: true
