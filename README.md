# Authentication-Application
A secure authentication system built with Spring Boot that handles user login, registration, and role-based access using JWT. It can be reused across multiple applications as a common authentication service.
